import{$,esc,nf,playerUrl,clubUrl,attachGlobalSearch,get,relative,labels}from'./v8-common.js';attachGlobalSearch($('#globalSearch'),$('#suggestions'));function linePlayer(p,i){return`<a class="line" href="${playerUrl(p)}"><span><b>#${i+1} ${esc(p.name)}</b><span class="small muted"> • ${esc(p.club_name||'Sans club')}</span></span><span><b>${nf(p.goals)}</b> buts</span></a>`}function lineClub(c,i){return`<a class="line" href="${clubUrl(c)}"><span><b>#${i+1} ${esc(c.name)}</b><span class="small muted"> • ${esc(labels[c.platform]||c.platform)}</span></span><span><b>${nf(c.skill)}</b> skill</span></a>`}function popularPlayer(p){return`<a class="line" href="${playerUrl(p)}"><span><b>${esc(p.name)}</b><span class="small muted"> • ${esc(p.club_name||'Sans club')}</span></span><span>${nf(p.views)} vue${Number(p.views)!==1?'s':''}</span></a>`}function popularClub(c){return`<a class="line" href="${clubUrl(c)}"><span><b>${esc(c.name)}</b><span class="small muted"> • ${esc(labels[c.platform]||c.platform)}</span></span><span>${nf(c.views)} vue${Number(c.views)!==1?'s':''}</span></a>`}function activeClub(c){const games=Number(c.games||0),wr=games?Number(c.wins||0)/games*100:0;return`<a class="card" style="padding:15px" href="${clubUrl(c)}"><div class="identity"><div class="crest">${esc(c.name?.[0]||'?')}</div><div><b>${esc(c.name)}</b><div class="small muted">${esc(labels[c.platform]||c.platform)}</div></div></div><div class="metrics" style="grid-template-columns:repeat(3,1fr);margin-bottom:0"><div class="metric"><b>${nf(games)}</b><span>Matchs</span></div><div class="metric"><b>${nf(c.wins)}</b><span>Victoires</span></div><div class="metric"><b>${wr.toFixed(1)}%</b><span>% V</span></div></div></a>`}async function load(){try{$('#activeClubs').innerHTML='<div class="skeleton" style="height:110px"></div><div class="skeleton" style="height:110px"></div><div class="skeleton" style="height:110px"></div>';const[s,pr,cr,active,m,pp,pc]=await Promise.all([get('/api/v8/stats'),get('/api/v8/rankings?type=player&metric=goals&minGames=3&limit=8'),get('/api/v8/rankings?type=club&metric=skill&minGames=3&limit=8'),get('/api/v8/rankings?type=club&metric=games&minGames=0&limit=6'),get('/api/matches'),get('/api/v8/popular?type=player&limit=8'),get('/api/v8/popular?type=club&limit=8')]);$('#heroPlayers').textContent=nf(s.players);$('#sPlayers').textContent=nf(s.players);$('#sClubs').textContent=nf(s.clubs);$('#sMatches').textContent=nf(s.matches);$('#sApps').textContent=nf(s.appearances);$('#topPlayers').innerHTML=(pr.items||[]).map(linePlayer).join('')||'<div class="empty">Aucune donnée</div>';$('#topClubs').innerHTML=(cr.items||[]).map(lineClub).join('')||'<div class="empty">Aucune donnée</div>';$('#activeClubs').innerHTML=(active.items||[]).map(activeClub).join('')||'<div class="empty">Aucun club actif.</div>';$('#popularPlayers').innerHTML=(pp.items||[]).map(popularPlayer).join('')||'<div class="empty">Les consultations apparaîtront ici.</div>';$('#popularClubs').innerHTML=(pc.items||[]).map(popularClub).join('')||'<div class="empty">Les consultations apparaîtront ici.</div>';$('#matches').innerHTML=(m.items||[]).slice(0,10).map(x=>`<div class="matchrow"><b>${esc(x.home_name||'—')}</b><strong>${nf(x.home_goals)}-${nf(x.away_goals)}</strong><b>${esc(x.away_name||'—')}</b><span class="small muted">${relative(x.ts)}</span></div>`).join('')||'<div class="empty">Aucun match</div>';$('#qUnresolved').textContent=nf(s.quality?.unresolvedClubs);$('#qDup').textContent=nf(s.quality?.duplicatePlayerGroups);$('#qOrphan').textContent=nf(s.quality?.orphanAppearances);$('#latestMatch').textContent=relative(s.latestMatch);$('#latestUpdate').textContent=relative(s.latestUpdate);$('#storage').textContent=s.storage==='postgres'?'PostgreSQL':'Local'}catch(e){console.error(e);$('#activeClubs').innerHTML='<div class="empty">Impossible de charger les données.</div>'}}load();